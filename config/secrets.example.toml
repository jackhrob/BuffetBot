# Copy to config/secrets.local.toml, which is ignored by Git.
# Replace the blank placeholders locally. Never put real keys in this example.
# The file is loaded only when explicitly supplied with --secrets.
[alpaca]
api_key = ""
api_secret = ""
